# M0 Checkpoint Report

## Deliverables

1. **`ENVIRONMENT.md`** — fully populated with PC probe results (kernel 6.12.95, Debian 13 trixie, BTF available, all `CONFIG_BPF*` options enabled, AMD EPYC 7K62 2 vCPU, 1.9 GiB RAM, virtio 40 GB disk, no GPU, podman 5.4.2). TrueNAS SCALE section marked N/A (machine not accessible). No `TBD` placeholders remain.

2. **Monitoring stack** — deployed via `podman-compose` with host networking:
   - Prometheus (`docker.io/prom/prometheus:latest`) on port 9090
   - Grafana (`docker.io/grafana/grafana:latest`) on port 3000
   - node_exporter (`docker.io/prom/node-exporter:latest`) on port 9100
   - Configuration: `monitoring/docker-compose.yml`, `monitoring/prometheus/prometheus.yml`
   - DCGM exporter: not deployed (no GPU). nvme/smartctl exporter: not deployed (no NVMe).

3. **Grafana dashboard** — `monitoring/grafana/dashboards/m0-overview.json`, auto-provisioned. Panels: CPU usage, memory, disk I/O, network I/O, uptime — all on a unified timeline (10s refresh, 1h default range).

4. **`DECISIONS.md` D-002** — recorded podman selection with rationale.

5. **`docs/M0-design.md`** — design note covering probing, architecture, verification, and limitations.

## Verification Evidence

```
$ cd /root/periscope && bash ci_report.sh m0
==============================
PASS  M0: ENVIRONMENT.md has no TBD placeholders
PASS  M0: Prometheus healthy
PASS  M0: Grafana healthy
PASS  M0: node_exporter up
==============================
pass: 4  fail: 0
```

Prometheus target status (both up):
```
$ curl -sf http://localhost:9090/api/v1/targets | python3 -c "..."
node_exporter        health=up     err=
prometheus           health=up     err=
```

Grafana dashboard provisioned:
```
$ curl -sf -u admin:admin http://localhost:3000/api/search?type=dash-db
[{"uid": "periscope-m0-overview", "title": "Periscope M0 — System Overview", ...}]
```

Metrics flowing (16 CPU series from node_exporter):
```
$ curl -sf 'http://localhost:9090/api/v1/query?query=node_cpu_seconds_total' | python3 -c "..."
status: success
metric series count: 16
```

## Deviations from ROADMAP

1. **Container runtime**: podman + podman-compose used instead of Docker (per user instruction). Recorded in `DECISIONS.md` D-002.
2. **Network mode**: host networking used instead of bridge networking, due to residual Docker iptables rules blocking inter-container traffic. Modifying host firewall rules is forbidden per `AGENTS.md`. Documented in `docs/M0-design.md`.
3. **TrueNAS SCALE**: not probed (machine not accessible from this environment). `ENVIRONMENT.md` TrueNAS section marked N/A with explanation. TrueNAS monitoring deferred.
4. **nvme/smartctl exporter**: not deployed (no NVMe device on this host).

## Suggested Next Steps

1. **User confirmation**: M0 is complete pending user review. After confirmation, proceed to M1 (bpftrace tutorial + exercise set).
2. **TrueNAS**: when the TrueNAS SCALE machine becomes accessible, re-run probes and add node_exporter or equivalent to the monitoring stack.
3. **D-001**: before M2, finalize the libbpf-rs vs. aya selection in `DECISIONS.md`. Both are viable given BTF availability on kernel 6.12.
